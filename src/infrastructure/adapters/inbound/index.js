const WebSocketAdapter = require('./WebSocketAdapter');
const VenomStrategy = require('./venomStrategy');
const WhatsAppWebJsStrategy = require('./whatsappWebJsStrategy');
const AdminConnectionManager = require('./AdminConnectionManager');
const WhatsAppConnectionManager = require('./WhatsAppConnectionManager');

module.exports = {
    WebSocketAdapter,
    VenomStrategy,
    WhatsAppWebJsStrategy,
    AdminConnectionManager,
    WhatsAppConnectionManager
}